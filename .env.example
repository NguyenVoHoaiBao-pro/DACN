# Copy file nay thanh .env (gitignore — khong commit mat khau)
# Local Docker MySQL (mac dinh khi khong co .env):
#   MYSQL_HOST=localhost  MYSQL_PORT=3306  MYSQL_USER=root  MYSQL_PASSWORD=

# --- Aiven MySQL (cung host/user/pass, moi service dung DB rieng trong application.yml) ---
MYSQL_HOST=mysql-11ae5c82-cjcakvalvajvjallh.i.aivencloud.com
MYSQL_PORT=28570
MYSQL_USER=avnadmin
MYSQL_PASSWORD=your_aiven_password_here

# SSL (bat buoc voi Aiven)
MYSQL_USE_SSL=true
MYSQL_REQUIRE_SSL=true
MYSQL_SSL_MODE=REQUIRED

# Tren Aiven da tao DB san — khong tu tao khi start app
MYSQL_CREATE_DB=false

# Ten DB tung service (da import tu database/schemas/):
#   catalog-service    -> electro_catalog_db
#   user-service       -> electro_user_db
#   cart-service       -> electro_cart_db
#   order-service      -> electro_order_db
#   review-service     -> electro_review_db
#   statistics-service -> electro_statistics_db
#   reco-service         -> electro_statistics_db (user_interactions)

# --- Redis Cloud (cloud.redis.io) — auth refresh token + statistics queue ---
# Cach 1: URL day du (uu tien — auth/statistics tu dong dung khi co REDIS_URL)
# redis:// = khong TLS (port 14085 cloud.redis.io thuong dung cach nay)
# rediss:// = co TLS
REDIS_URL=redis://default:YOUR_PASSWORD@YOUR_HOST.db.redis.io:14085
#
# Cach 2: Tach bien (tu dashboard Redis Cloud)
REDIS_HOST=YOUR_HOST.cloud.redislabs.com
REDIS_PORT=16379
REDIS_USERNAME=default
REDIS_PASSWORD=your_redis_password_here
REDIS_SSL=false
# Neu dung REDIS_URL thi scheme redis:// = false, rediss:// = true

# JWT (cung secret cho auth-service, gateway, user/cart/order)
JWT_SECRET=your_jwt_signing_secret_min_32_chars

# --- Chatbot RAG (chatbot-service — NVIDIA NIM + Pinecone cloud) ---
# NVIDIA: https://build.nvidia.com/
# NVIDIA_API_KEY=nvapi-...
# NVIDIA_CHAT_MODEL=meta/llama-3.1-8b-instruct
# NVIDIA_EMBEDDING_MODEL=nvidia/nv-embed-v1
#
# Pinecone (https://app.pinecone.io/) — KHONG can Docker Chroma nua
# Tao index: ten electro-store-products, metric cosine, dimensions 4096 (khop nvidia/nv-embed-v1)
# PINECONE_API_KEY=pcsk-...
# PINECONE_INDEX_NAME=electro-store-products
# Spring AI 1.0 + Serverless: chi can API key + index name
# PINECONE_NAMESPACE=
#
# Phase 2 RAG — hybrid + rerank (xem docs/CHATBOT_PINECONE.md)
# RAG_HYBRID_ENABLED=true
# RAG_HYBRID_ALPHA=0.75
# RAG_RETRIEVAL_TOP_K=40
# RAG_RERANK_ENABLED=true
# RAG_RERANK_MODEL=bge-reranker-v2-m3
# RAG_TOP_K=4

# Tai khoan test API (user khang_test trong DB seed — doi neu ban doi mat khau)
TEST_USERNAME=khang_test
TEST_PASSWORD=your_test_password_here

# Google Sign-In (optional) — Client ID tu Google Cloud Console
GOOGLE_CLIENT_ID=your_google_oauth_client_id.apps.googleusercontent.com

# --- VNPay Sandbox (order-service) ---
# Portal: https://sandbox.vnpayment.vn/merchantv2/  (nguyenbibi280@gmail.com)
# Return URL: http://localhost:8080/api/payment/vnpay/return
# IPN URL:    http://localhost:8080/api/payment/vnpay/ipn
# Gia tri mac dinh da co trong config-repo/application.yml; chi can set .env neu muon ghi de
VNPAY_TMN_CODE=1T0D4TUS
VNPAY_HASH_SECRET=UNVQ0K206FIK1H2PNV667HPWJ2ARZMWD
# VNPAY_PAY_URL=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
# VNPAY_RETURN_URL=http://localhost:8080/api/payment/vnpay/return

# --- reco-service (Python FastAPI, port 5003, Eureka: reco-service) ---
# statistics-service goi qua LoadBalancer: http://reco-service
# RECO_SERVICE_URL=http://localhost:5003
# AI_PORT=5003
# EUREKA_SERVER_URL=http://localhost:8761/eureka/

# --- Distributed Tracing (Zipkin UI — Micrometer OTEL) ---
# UI: http://localhost:9411  |  docker compose up -d zipkin
# ZIPKIN_ENDPOINT=http://localhost:9411/api/v2/spans
# TRACING_SAMPLING_PROBABILITY=1.0

# PAYMENT_FRONTEND_SUCCESS_URL=http://localhost:5173/payment/success
# PAYMENT_FRONTEND_FAIL_URL=http://localhost:5173/payment/fail
# PAYMENT_BACKEND_BASE_URL=http://localhost:8080
