package com.electro.order.repository;

import com.electro.order.entity.SalesKpiConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SalesKpiConfigRepository extends JpaRepository<SalesKpiConfig, Integer> {
}
