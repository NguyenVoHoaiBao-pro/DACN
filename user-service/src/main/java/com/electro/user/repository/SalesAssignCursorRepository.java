package com.electro.user.repository;

import com.electro.user.entity.SalesAssignCursor;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SalesAssignCursorRepository extends JpaRepository<SalesAssignCursor, Integer> {
}
